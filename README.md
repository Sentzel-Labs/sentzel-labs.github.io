# sentzel-labs.github.io
Website of Sentzel Labs.

1. **Version Management**: Each version lives in `versions/[VERSION_NAME]/`
2. **Active Version**: Defined in `versions.json`
3. **Auto-Deploy**: Push to `main` branch triggers automatic build and deployment
